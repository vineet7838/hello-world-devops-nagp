pipeline {

 agent any
 
   tools {
     maven "Maven3"
   }
   
   options {
     timestamps()
     skipDefaultCheckout()
   }
   
   stages {
      stage('checkout') {
        step {
          echo "checkout"
        }
      }
   }
}