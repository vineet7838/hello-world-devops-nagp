pipeline {

 agent any
 
 
   tools {
     maven "Maven3"
   }
   
   options {
     timestamps()
     skipDefaultCheckout()
   }
   
   parameters {
  choice( 
         name: 'Environment',
         choices: ['Develop', Feature],
         description: 'Environment choices'
  )
 }
 
   
   stages {
      stage('checkout') {
        steps {
          echo "checkout"
          git checkout []
        }
      }
   }
}